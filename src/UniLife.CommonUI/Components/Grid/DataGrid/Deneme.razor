@page "/deneme"
@page "/scheduler/month-agenda-view"
@using Syncfusion.Blazor.Schedule
@inherits SampleBaseComponent;
<div class="col-lg-12 control-section">
    <div class="schedule-wrapper">
        <SfSchedule TValue="ScheduleData.FifaEventsData" Width="100%" Height="510px" @bind-SelectedDate="@CurrentDate">
            <ScheduleViews>
                <ScheduleView Option="View.MonthAgenda"></ScheduleView>
            </ScheduleViews>
            <ScheduleEventSettings DataSource="@DataSource"></ScheduleEventSettings>
            <ScheduleEvents TValue="ScheduleData.FifaEventsData" ActionCompleted="@OnActionCompleted"></ScheduleEvents>
        </SfSchedule>
    </div>
</div>
@*custom code start*@
<style>
    @@media (min-width: 481px) {

        /*Küçük gelmesi için*/
        /*.schedule-wrapper {
            margin: 0 25%;
        }*/

    }
</style>
@*custom code end*@
@code{
    private DateTime CurrentDate = new DateTime(2020, 1, 22);
    public List<ScheduleData.FifaEventsData> DataSource = new ScheduleData().GetFifaEventsData();

    public async Task OnActionCompleted(Syncfusion.Blazor.Schedule.ActionEventArgs<ScheduleData.FifaEventsData> args)
    {
        if (args.RequestType == "eventCreated")
        {

        }
        else if (args.RequestType == "eventChanged")
        {

        }
        else if (args.RequestType == "eventRemoved")
        {

        }
    }
}